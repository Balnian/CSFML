name: Build

on: [push]


jobs:
  build:

    runs-on: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run:  |
        cd tools/nuget
        ls -al
        ./build.linux.sh macos-x64
